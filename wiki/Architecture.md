# Архитектура WORK21

## 🏗️ Общая схема

```
┌─────────────────────────────────────────────────────────────┐
│                      FRONTEND (Next.js)                     │
│  Landing • Студенты • Заказчики • Dashboard • Job Connect   │
└─────────────────────────┬───────────────────────────────────┘
                          │ REST API (JSON)
┌─────────────────────────▼───────────────────────────────────┐
│                    BACKEND (FastAPI)                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────────┐  │
│  │   Auth   │  │ Projects │  │ Ratings  │  │ Job Connect │  │
│  └──────────┘  └──────────┘  └──────────┘  └─────────────┘  │
│  ┌─────────────────────────────────────────────────────────┐│
│  │                    AI AGENTS                            ││
│  │  Task Analyst • Talent Matcher • Legal Assistant        ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────┐
│                   DATABASE (PostgreSQL)                     │
│        Users • Projects • Tasks • Ratings • Contracts       │
└─────────────────────────────────────────────────────────────┘
```

## 🛠️ Технологический стек

### Frontend

| Технология | Версия | Назначение |
|------------|--------|------------|
| Next.js | 14.2 | React-фреймворк с SSR |
| React | 18.2 | UI библиотека |
| TypeScript | 5.3 | Типизация |
| Tailwind CSS | 3.4 | Стилизация |
| Lucide React | 0.350 | Иконки |

### Backend

| Технология | Версия | Назначение |
|------------|--------|------------|
| Python | 3.10+ | Язык программирования |
| FastAPI | 0.109 | Web-фреймворк |
| SQLAlchemy | 2.0 | ORM |
| Pydantic | 2.6 | Валидация данных |
| python-jose | 3.3 | JWT токены |
| bcrypt | 4.0 | Хеширование паролей |

### Database

| Среда | Технология |
|-------|------------|
| Development | SQLite |
| Production | PostgreSQL 15+ |

## 📊 Модель данных

### Users (Пользователи)

```python
class User:
    id: int                    # PK
    email: str                 # Уникальный
    hashed_password: str
    first_name: str
    last_name: str
    role: UserRole             # student | customer | admin
    bio: str | None
    skills: str | None         # JSON массив
    avatar_url: str | None
    rating_score: float        # Средний рейтинг
    completed_projects: int
    is_active: bool
    is_verified: bool
    created_at: datetime
```

### Projects (Проекты)

```python
class Project:
    id: int                    # PK
    title: str
    description: str
    requirements: str | None
    budget: float
    deadline: datetime | None
    tech_stack: str | None     # JSON массив
    status: ProjectStatus      # draft | open | in_progress | completed
    customer_id: int           # FK -> Users
    generated_spec: str | None # ТЗ от AI
    created_at: datetime
```

### Applications (Заявки)

```python
class Application:
    id: int                    # PK
    project_id: int            # FK -> Projects
    student_id: int            # FK -> Users
    cover_letter: str | None
    proposed_rate: float | None
    status: ApplicationStatus  # pending | accepted | rejected
    created_at: datetime
```

### Contracts (Договоры)

```python
class Contract:
    id: int                    # PK
    project_id: int            # FK -> Projects
    customer_id: int           # FK -> Users
    student_id: int            # FK -> Users
    total_amount: float
    platform_fee: float        # 10%
    student_payment: float
    terms: str                 # Текст договора
    status: ContractStatus
    created_at: datetime
```

## 🔐 Безопасность

### Аутентификация

- **JWT токены** с ограниченным сроком действия
- **bcrypt** для хеширования паролей
- **OAuth2** с Bearer token схемой

### Авторизация (RBAC)

| Роль | Возможности |
|------|-------------|
| `student` | Просмотр проектов, подача заявок, выполнение |
| `customer` | Создание проектов, выбор исполнителей, оценки |
| `admin` | Полный доступ, модерация |

### Защита данных

- HTTPS в production
- CORS настройки
- SQL injection защита (SQLAlchemy ORM)
- XSS защита (React escaping)

## 🔄 Потоки данных

### Создание проекта

```
1. Заказчик → POST /projects → Создание черновика
2. Task Analyst → Анализ → Генерация ТЗ
3. Заказчик → POST /projects/{id}/publish → Публикация
4. Talent Matcher → Поиск кандидатов → Уведомления
```

### Выполнение проекта

```
1. Студент → POST /projects/{id}/apply → Заявка
2. Заказчик → PUT /applications/{id} → Принятие
3. Legal Assistant → Генерация договора
4. Студент → Выполнение → Сдача
5. Заказчик → POST /ratings → Оценка
6. Система → Обновление рейтинга + Оплата
```

## 📈 Масштабирование

### Горизонтальное масштабирование

- Stateless backend (можно запускать несколько инстансов)
- Redis для кэширования сессий
- Celery для фоновых задач (AI-агенты)

### База данных

- PostgreSQL read replicas для чтения
- Connection pooling (SQLAlchemy)
- Индексы на часто используемых полях

---

[[Home]] | [[API Reference]] | [[AI Agents]]

