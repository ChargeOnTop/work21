# 📋 План миграции с SQLite на PostgreSQL

## 🎯 Обзор

Этот документ содержит полный список задач для перехода проекта WORK21 с SQLite на PostgreSQL.

---

## ✅ Текущее состояние

### Что уже готово:
- ✅ Docker Compose настроен для PostgreSQL (`docker/docker-compose.yml`)
- ✅ `asyncpg` уже установлен в `requirements.txt`
- ✅ Модели используют стандартные типы SQLAlchemy (совместимы с PostgreSQL)
- ✅ Alembic установлен в зависимостях

### Что нужно изменить:
- ❌ Дефолтное значение `DATABASE_URL` указывает на SQLite
- ❌ Логика преобразования URL для синхронного движка работает только для SQLite
- ❌ Скрипт `db_status.py` использует `sqlite3` напрямую
- ❌ Alembic не настроен (нет миграций)
- ❌ Документация упоминает SQLite

---

## 📝 Список задач

### 1. Конфигурация базы данных

#### 1.1. Изменить дефолтное значение DATABASE_URL
**Файл:** `backend/app/core/config.py`

**Текущее:**
```python
database_url: str = "sqlite+aiosqlite:///./work21.db"
```

**Нужно изменить на:**
```python
database_url: str = "postgresql+asyncpg://work21:work21password@localhost:5433/work21"
```

**Примечание:** Для локальной разработки можно оставить SQLite как опцию, но PostgreSQL должен быть по умолчанию.

---

#### 1.2. Исправить логику преобразования URL для синхронного движка
**Файл:** `backend/app/core/database.py`

**Текущее:**
```python
# Преобразуем sqlite+aiosqlite -> sqlite
sync_database_url = settings.database_url.replace("+aiosqlite", "").replace("+asyncpg", "")
```

**Проблема:** Логика не учитывает, что для PostgreSQL нужно преобразовать `postgresql+asyncpg://` в `postgresql://` (или `postgresql+psycopg2://` для синхронного подключения).

**Нужно:**
- Добавить правильное преобразование для PostgreSQL
- Установить `psycopg2` или `psycopg2-binary` для синхронного подключения SQLAdmin
- Обновить `requirements.txt`

---

### 2. Зависимости

#### 2.1. Добавить psycopg2 для синхронного подключения
**Файл:** `backend/requirements.txt`

**Добавить:**
```
psycopg2-binary==2.9.9
```

**Примечание:** `psycopg2-binary` проще установить, но для production лучше использовать `psycopg2` с компиляцией.

---

#### 2.2. Опционально: Удалить aiosqlite
**Файл:** `backend/requirements.txt`

**Решение:** Можно оставить для обратной совместимости или удалить, если полностью переходим на PostgreSQL.

---

### 3. Настройка Alembic (миграции)

#### 3.1. Инициализировать Alembic
**Команда:**
```bash
cd backend
alembic init alembic
```

**Создаст:**
- `alembic.ini` - конфигурация Alembic
- `alembic/` - папка с миграциями

---

#### 3.2. Настроить alembic.ini
**Файл:** `backend/alembic.ini`

**Изменить:**
```ini
# Было:
sqlalchemy.url = driver://user:pass@localhost/dbname

# Станет (или использовать переменную окружения):
sqlalchemy.url = postgresql+asyncpg://work21:work21password@localhost:5433/work21
```

**Лучше:** Использовать переменную окружения из `.env`:
```python
# В alembic/env.py
from app.core.config import settings
config.set_main_option("sqlalchemy.url", settings.database_url.replace("+asyncpg", ""))
```

---

#### 3.3. Настроить env.py для Alembic
**Файл:** `backend/alembic/env.py`

**Нужно:**
- Импортировать модели для автогенерации миграций
- Настроить подключение к базе данных из настроек приложения
- Убедиться, что используется синхронный драйвер (не asyncpg)

**Пример:**
```python
from app.core.config import settings
from app.core.database import Base
from app.models import *  # Импорт всех моделей

# Преобразуем async URL в sync для Alembic
sync_url = settings.database_url.replace("+asyncpg", "+psycopg2")
config.set_main_option("sqlalchemy.url", sync_url)

target_metadata = Base.metadata
```

---

#### 3.4. Создать начальную миграцию
**Команда:**
```bash
cd backend
alembic revision --autogenerate -m "Initial migration"
```

**Проверить:** Сгенерированный файл миграции должен содержать создание всех таблиц.

---

#### 3.5. Применить миграции
**Команда:**
```bash
alembic upgrade head
```

---

### 4. Обновить скрипты и утилиты

#### 4.1. Переписать db_status.py для PostgreSQL
**Файл:** `backend/db_status.py`

**Текущее:** Использует `sqlite3` напрямую

**Нужно:**
- Использовать SQLAlchemy или `psycopg2` для подключения
- Изменить SQL запросы (например, `sqlite_master` → `information_schema.tables`)
- Обновить логику работы с базой данных

**Альтернатива:** Использовать SQLAlchemy для кроссплатформенности.

---

#### 4.2. Обновить init_db() (опционально)
**Файл:** `backend/app/core/database.py`

**Текущее:** Использует `Base.metadata.create_all()`

**Рекомендация:** 
- В production использовать миграции Alembic
- `create_all()` оставить только для разработки/тестов
- Добавить проверку, что миграции применены

---

### 5. Обновить документацию

#### 5.1. Обновить README.md
**Файл:** `README.md`

**Изменить:**
- Убрать упоминание SQLite как дефолтной БД
- Обновить секцию "Технологический стек"
- Обновить инструкции по запуску

---

#### 5.2. Обновить ИНСТРУКЦИЯ_ПО_ЗАПУСКУ.md
**Файл:** `ИНСТРУКЦИЯ_ПО_ЗАПУСКУ.md`

**Изменить:**
- Обновить примеры `.env` файлов
- Указать PostgreSQL как основную БД
- Добавить инструкции по настройке PostgreSQL локально (если нужно)
- Обновить секцию "Проверка работоспособности"

---

#### 5.3. Обновить wiki/Getting-Started.md
**Файл:** `wiki/Getting-Started.md`

**Изменить:**
- Обновить примеры конфигурации
- Убрать упоминания SQLite

---

#### 5.4. Обновить docs/architecture.md
**Файл:** `docs/architecture.md`

**Изменить:**
- Указать PostgreSQL как основную БД
- Убрать упоминания SQLite для development

---

### 6. Проверка моделей на совместимость

#### 6.1. Проверить типы данных
**Файлы:** Все файлы в `backend/app/models/`

**Проверить:**
- ✅ `String`, `Text`, `Integer`, `Float`, `DateTime` - совместимы
- ✅ `Enum` - нужно проверить, что используется `SQLEnum` (уже используется)
- ✅ `ForeignKey` - совместимы
- ✅ `Mapped` типы - совместимы

**Статус:** ✅ Модели уже совместимы с PostgreSQL

---

#### 6.2. Проверить constraints и индексы
**Проверить:**
- Уникальные индексы (`unique=True`)
- Внешние ключи
- Проверочные ограничения (если есть)

**Статус:** ✅ Все constraints используют стандартный синтаксис SQLAlchemy

---

### 7. Тестирование

#### 7.1. Создать тестовую базу данных
**Команда:**
```bash
# Локально
createdb work21_test

# Или через Docker
docker exec -it work21-db psql -U work21 -c "CREATE DATABASE work21_test;"
```

---

#### 7.2. Протестировать подключение
**Проверить:**
- Подключение к базе данных
- Создание таблиц (через миграции)
- CRUD операции
- Relationships между моделями
- SQLAdmin панель

---

#### 7.3. Миграция данных (если есть существующие данные)
**Если есть данные в SQLite:**

1. Экспортировать данные из SQLite
2. Преобразовать формат (если нужно)
3. Импортировать в PostgreSQL

**Инструменты:**
- `pgloader` - автоматическая миграция
- Ручной экспорт/импорт через SQLAlchemy

---

### 8. Обновить Docker конфигурацию (опционально)

#### 8.1. Проверить docker-compose.yml
**Файл:** `docker/docker-compose.yml`

**Статус:** ✅ Уже настроен для PostgreSQL

**Проверить:**
- Переменные окружения
- Healthcheck
- Volumes для персистентности данных

---

#### 8.2. Добавить init скрипты (опционально)
**Создать:** `docker/init-db.sql` или использовать init скрипты PostgreSQL

**Для:**
- Создания расширений (если нужно)
- Начальных данных
- Пользователей и прав

---

### 9. Обновить .gitignore

#### 9.1. Добавить исключения для PostgreSQL
**Файл:** `.gitignore` или `backend/.gitignore`

**Добавить:**
```
# PostgreSQL
*.sql.backup
*.dump
.pgpass
```

**Удалить (если есть):**
```
# SQLite
*.db
*.db-journal
```

---

### 10. CI/CD (если используется)

#### 10.1. Обновить тестовое окружение
**Проверить:**
- GitHub Actions / GitLab CI конфигурации
- Тестовые базы данных
- Скрипты развертывания

---

## 🚀 Порядок выполнения

### Этап 1: Подготовка (низкий риск)
1. ✅ Добавить `psycopg2-binary` в `requirements.txt`
2. ✅ Обновить логику преобразования URL в `database.py`
3. ✅ Изменить дефолтное значение `DATABASE_URL` в `config.py`

### Этап 2: Настройка миграций (средний риск)
4. ✅ Инициализировать Alembic
5. ✅ Настроить `alembic.ini` и `env.py`
6. ✅ Создать начальную миграцию
7. ✅ Протестировать миграции на тестовой БД

### Этап 3: Обновление утилит (низкий риск)
8. ✅ Переписать `db_status.py`
9. ✅ Обновить `init_db()` (опционально)

### Этап 4: Тестирование (критично)
10. ✅ Протестировать на локальной PostgreSQL
11. ✅ Протестировать через Docker Compose
12. ✅ Проверить все CRUD операции
13. ✅ Проверить SQLAdmin панель

### Этап 5: Документация (низкий риск)
14. ✅ Обновить всю документацию
15. ✅ Обновить примеры конфигурации

### Этап 6: Миграция данных (если нужно)
16. ✅ Экспортировать данные из SQLite (если есть)
17. ✅ Импортировать в PostgreSQL

---

## ⚠️ Важные замечания

### Различия SQLite и PostgreSQL

1. **Типы данных:**
   - SQLite: слабая типизация
   - PostgreSQL: строгая типизация
   - ✅ Модели уже используют правильные типы

2. **Enum:**
   - SQLite: хранится как строка
   - PostgreSQL: нативный тип ENUM
   - ✅ Используется `SQLEnum`, что правильно

3. **Автоинкремент:**
   - SQLite: `AUTOINCREMENT`
   - PostgreSQL: `SERIAL` или `IDENTITY`
   - ✅ SQLAlchemy автоматически обрабатывает

4. **Boolean:**
   - SQLite: INTEGER (0/1)
   - PostgreSQL: BOOLEAN
   - ✅ SQLAlchemy автоматически обрабатывает

5. **Даты и время:**
   - SQLite: TEXT или REAL
   - PostgreSQL: TIMESTAMP, DATE, TIME
   - ✅ Используется `DateTime`, что правильно

6. **Транзакции:**
   - SQLite: ограниченная поддержка
   - PostgreSQL: полная поддержка ACID
   - ✅ Код использует сессии, что совместимо

### Потенциальные проблемы

1. **Case sensitivity:**
   - SQLite: не чувствителен к регистру имен таблиц
   - PostgreSQL: чувствителен (использует нижний регистр)
   - ✅ Имена таблиц уже в нижнем регистре

2. **Кавычки в идентификаторах:**
   - SQLite: более терпим
   - PostgreSQL: строгий
   - ✅ SQLAlchemy обрабатывает автоматически

3. **Ограничения:**
   - SQLite: некоторые ограничения не поддерживаются
   - PostgreSQL: полная поддержка
   - ✅ Код использует стандартные ограничения

---

## 📊 Чеклист выполнения

- [x] 1.1. Изменить дефолтное значение DATABASE_URL ✅
- [x] 1.2. Исправить логику преобразования URL для синхронного движка ✅
- [x] 2.1. Добавить psycopg2-binary в requirements.txt ✅
- [ ] 2.2. (Опционально) Удалить aiosqlite (оставлено для обратной совместимости)
- [x] 3.1. Инициализировать Alembic ✅
- [x] 3.2. Настроить alembic.ini ✅
- [x] 3.3. Настроить env.py для Alembic ✅
- [ ] 3.4. Создать начальную миграцию (требует запущенной БД)
- [ ] 3.5. Применить миграции (требует запущенной БД)
- [x] 4.1. Переписать db_status.py ✅
- [ ] 4.2. Обновить init_db() (опционально, оставлено как есть)
- [x] 5.1. Обновить README.md ✅
- [x] 5.2. Обновить ИНСТРУКЦИЯ_ПО_ЗАПУСКУ.md ✅
- [x] 5.3. Обновить wiki/Getting-Started.md ✅
- [x] 5.4. Обновить docs/architecture.md ✅
- [x] 6.1. Проверить типы данных моделей ✅ (совместимы)
- [x] 6.2. Проверить constraints и индексы ✅ (совместимы)
- [ ] 7.1. Создать тестовую базу данных (выполнить вручную)
- [ ] 7.2. Протестировать подключение (выполнить вручную)
- [ ] 7.3. Миграция данных (если нужно)
- [x] 8.1. Проверить docker-compose.yml ✅ (уже настроен)
- [ ] 8.2. Добавить init скрипты (опционально)
- [ ] 9.1. Обновить .gitignore (опционально)
- [ ] 10.1. Обновить CI/CD (если используется)

---

## 🔗 Полезные ссылки

- [SQLAlchemy PostgreSQL](https://docs.sqlalchemy.org/en/20/dialects/postgresql.html)
- [Alembic Tutorial](https://alembic.sqlalchemy.org/en/latest/tutorial.html)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)
- [asyncpg Documentation](https://magicstack.github.io/asyncpg/)

---

**Дата создания:** 2024  
**Статус:** ✅ Основные задачи выполнены

## ✅ Выполнено

Основная миграция завершена! Выполнены следующие задачи:

1. ✅ Добавлен `psycopg2-binary` в зависимости
2. ✅ Изменено дефолтное значение `DATABASE_URL` на PostgreSQL
3. ✅ Исправлена логика преобразования URL для синхронного движка
4. ✅ Инициализирован и настроен Alembic
5. ✅ Переписан `db_status.py` для поддержки PostgreSQL
6. ✅ Обновлена вся документация

## 📝 Осталось выполнить вручную

Следующие задачи требуют запущенной базы данных PostgreSQL:

1. **Создать начальную миграцию:**
   ```bash
   cd backend
   alembic revision --autogenerate -m "Initial migration"
   ```

2. **Применить миграции:**
   ```bash
   alembic upgrade head
   ```

3. **Протестировать подключение** и работу приложения

После выполнения этих шагов миграция будет полностью завершена!

