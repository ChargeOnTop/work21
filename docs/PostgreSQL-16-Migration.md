# Миграция на PostgreSQL 16

## ✅ Обновление завершено

Проект WORK21 обновлен для работы с PostgreSQL 16.

---

## 📋 Что было изменено

### 1. Docker Compose
- Обновлен образ: `postgres:15-alpine` → `postgres:16-alpine`

### 2. Документация
- Обновлен README.md (требования к версии PostgreSQL)
- Обновлен wiki/Architecture.md
- Обновлены инструкции по установке

### 3. Совместимость
- ✅ Dockerfile.backend совместим (libpq-dev работает с любой версией)
- ✅ SQLAlchemy 2.0 полностью совместим с PostgreSQL 16
- ✅ asyncpg поддерживает PostgreSQL 16
- ✅ psycopg2-binary поддерживает PostgreSQL 16

---

## 🚀 Как обновить существующую установку

### Вариант 1: Чистая установка (рекомендуется для разработки)

Если у вас нет важных данных в базе:

```bash
cd docker

# Остановить контейнеры
docker-compose down

# Удалить volume с данными PostgreSQL 15
docker volume rm docker_postgres_data

# Запустить с PostgreSQL 16
docker-compose up -d db

# Применить миграции
docker exec -it work21-backend alembic upgrade head
```

### Вариант 2: Обновление существующей базы данных

Если у вас есть важные данные:

#### Шаг 1: Создать резервную копию

```bash
# Создать бэкап
docker exec -it work21-db pg_dump -U work21 work21 > backup_$(date +%Y%m%d).sql

# Или через Docker volume
docker run --rm -v docker_postgres_data:/data -v $(pwd):/backup \
  alpine tar czf /backup/postgres_backup.tar.gz /data
```

#### Шаг 2: Обновить docker-compose.yml

Образ уже обновлен на `postgres:16-alpine`.

#### Шаг 3: Остановить и пересоздать контейнер

```bash
cd docker

# Остановить контейнеры
docker-compose down

# Удалить старый контейнер (данные в volume сохранятся)
docker rm work21-db

# Запустить новый контейнер с PostgreSQL 16
docker-compose up -d db
```

#### Шаг 4: Обновить базу данных

PostgreSQL 16 может автоматически обновить данные с версии 15, но рекомендуется проверить:

```bash
# Проверить версию PostgreSQL
docker exec -it work21-db psql -U work21 -d work21 -c "SELECT version();"

# Проверить целостность данных
docker exec -it work21-db psql -U work21 -d work21 -c "\dt"

# Проверить миграции
docker exec -it work21-backend alembic current
```

#### Шаг 5: Восстановить данные (если нужно)

```bash
# Если что-то пошло не так, восстановите из бэкапа
docker exec -i work21-db psql -U work21 -d work21 < backup_YYYYMMDD.sql
```

---

## ⚠️ Важные замечания

### Совместимость данных

PostgreSQL 16 полностью совместим с данными из PostgreSQL 15. Автоматическое обновление происходит при первом запуске.

### Изменения в PostgreSQL 16

Основные улучшения:
- Улучшенная производительность
- Новые функции JSON
- Улучшенная работа с логированием
- Оптимизация запросов

**Полный список изменений:** [PostgreSQL 16 Release Notes](https://www.postgresql.org/docs/16/release-16.html)

### Проверка после обновления

```bash
# 1. Проверить версию
docker exec -it work21-db psql -U work21 -d work21 -c "SELECT version();"

# 2. Проверить таблицы
docker exec -it work21-db psql -U work21 -d work21 -c "\dt"

# 3. Проверить подключение приложения
docker exec -it work21-backend python db_status.py

# 4. Проверить миграции
docker exec -it work21-backend alembic current
```

---

## 🔧 Откат на PostgreSQL 15 (если нужно)

Если возникнут проблемы:

```bash
cd docker

# Изменить в docker-compose.yml:
# image: postgres:16-alpine → image: postgres:15-alpine

# Пересоздать контейнер
docker-compose down
docker-compose up -d db
```

---

## 📚 Дополнительные ресурсы

- [PostgreSQL 16 Documentation](https://www.postgresql.org/docs/16/)
- [PostgreSQL 16 Release Notes](https://www.postgresql.org/docs/16/release-16.html)
- [Миграция с PostgreSQL 15 на 16](https://www.postgresql.org/docs/16/upgrading.html)

---

**Дата обновления:** 2024  
**Статус:** ✅ Обновление завершено

